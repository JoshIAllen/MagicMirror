DB commands:

    rake db:create
    rake db:migrate
    rake db:seed
    
run command: $ rails server -p $PORT -b $IP

To get code from github
1. in console, cd into yhe magicmirror project
2. $ git pull https://github.com/JoshIAllen/MagicMirror

To push code updates to github
1. save all modified files
2. cd into magic mirror directory
3. $ git add .
4. $ git commit -m "a message"
5. $ git push origin master